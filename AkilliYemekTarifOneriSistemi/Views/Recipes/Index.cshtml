@model List<AkilliYemekTarifOneriSistemi.Models.Recipe>

@{
    ViewData["Title"] = "Tarifler";
}

<h2 class="mb-4 fw-bold">🍽️ Tarifler</h2>

<div class="row g-4">
    @foreach (var r in Model)
    {
        <div class="col-lg-4 col-md-6">
            <div class="card recipe-card border-0 shadow-sm">

                <div class="position-relative">
                    <img src="@(!string.IsNullOrEmpty(r.ImageUrl) ? r.ImageUrl : "https://via.placeholder.com/400x300")"
                         class="w-100 recipe-img" />

                    <div class="position-absolute top-0 end-0 m-3 badge-soft">
                        ⏱ @r.CookingTime dk
                    </div>
                </div>

                <div class="card-body">
                    <h5 class="fw-bold">@r.Title</h5>
                    <p class="text-muted small">@r.Description</p>

                    <a asp-action="Details"
                       asp-route-id="@r.Id"
                       class="btn btn-outline-dark w-100 rounded-pill">
                        Detayları Gör →
                    </a>
                </div>

            </div>
        </div>
    }
</div>
